<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Drinks Index Page</title>
</head>
<body>
    <h1>Welcome to GitPub</h1> 
    <style type="text/css">
        body {
            color: blanchedalmond;
            background-color: steelblue;
        }
    </style>
    <ul>
        <% for (let drink in allDrinks) { %>
        <li>
           <a href="/drinks/<%=drink%>"><%=`${allDrinks[drink].name.charAt(0).toUpperCase()}`%><%=`${allDrinks[drink].name.slice(1)}`%></a>
        </li>
        
        <% } %>
    </ul>
</body>
</html>